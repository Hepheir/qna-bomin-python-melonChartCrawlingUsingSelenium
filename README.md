

```bash
# Set up dev env.

git init

poetry init

poetry add Seleniumn
poetry add pandas
poetry add requests

# Download chromedriver

open /etc/paths

# Add chromedriver dir to paths.

chmod +x chromedriver

poetry run __init__.py
```
